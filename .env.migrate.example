# Migration database connection. Use a separate, more privileged identity.
# Keep real values outside the repository.
MIGRATION_DB_HOST=127.0.0.1
MIGRATION_DB_PORT=3306
MIGRATION_DB_NAME=reqsheet_dev
MIGRATION_DB_USER=reqsheet_migrator
MIGRATION_DB_PASSWORD=replace-with-migration-secret
